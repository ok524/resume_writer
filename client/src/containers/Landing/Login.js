import React from 'react';

import LoginForm from '../../components/AccountProcess/Login/LoginForm';

const Login = ({ match }) => {
    return(
        <div>
            <LoginForm />
        </div>
    )
};

export default Login;