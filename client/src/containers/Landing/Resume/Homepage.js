import React from 'react';

import ResumeHome from '../../../components/Resume/Homepage/ResumeHome';

const Homepage = ({ match }) => {
    return(
        <ResumeHome />
    )
};

export default Homepage;